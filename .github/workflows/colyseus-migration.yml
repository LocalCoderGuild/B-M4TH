name: Colyseus Migration Pipeline

on:
  pull_request:
  push:
    branches:
      - main
      - master
      - develop
  workflow_dispatch:

concurrency:
  group: colyseus-migration-${{ github.ref }}
  cancel-in-progress: true

jobs:
  baseline-engine:
    name: Baseline Engine Gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: cd apps/game && bun install --frozen-lockfile

      - name: Typecheck + full tests
        run: cd apps/game && bun run ci

  migration-readiness:
    name: Migration Readiness Gate
    runs-on: ubuntu-latest
    needs: baseline-engine
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: cd apps/game && bun install --frozen-lockfile

      - name: Engine regression tests (must stay green during migration)
        run: cd apps/game && bun run ci:engine

      - name: Optional Colyseus smoke tests
        shell: bash
        run: |
          if [ -d apps/game/test/colyseus ]; then
            echo "Found apps/game/test/colyseus, running smoke suite..."
            cd apps/game && bun test test/colyseus
          else
            echo "No apps/game/test/colyseus directory yet. Skipping Colyseus smoke tests."
          fi

  build-package:
    name: Package Gate
    runs-on: ubuntu-latest
    needs: migration-readiness
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: cd apps/game && bun install --frozen-lockfile

      - name: Validate lock + scripts
        run: |
          cd apps/game && bun run typecheck
          cd apps/game && bun run ci:engine
